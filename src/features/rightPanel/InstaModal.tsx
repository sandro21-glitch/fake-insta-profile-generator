import ModalDialog from "./modalContent/ModalDialog";

const InstaModal = () => {
  return (
    <div className="w-[360px] max-w-[1000px] mx-auto">
      <ModalDialog />
    </div>
  );
};

export default InstaModal;
